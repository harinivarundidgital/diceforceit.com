---
import InputCard from './InputCard.astro';

const inputs = [
  { id: 'docs', title: 'Knowledge & Contracts', count: '300K+', icon: 'file-text' },
  { id: 'crm', title: 'CRM & Financial Data', count: '350K+', icon: 'dollar-sign' },
  { id: 'emails', title: 'Emails & Communications', count: '250K+', icon: 'mail' },
  { id: 'market', title: 'Real-Time APIs & Feeds', count: '1M+', icon: 'globe' },
  { id: 'cloud', title: 'Cloud & Infrastructure', count: '3M+', icon: 'cloud' },
];
---

<div class="eai-panel-inputs flex flex-col h-full" aria-label="Section 1 Enterprise Inputs">
  <div class="eai-panel-title flex items-center justify-center text-center w-full">
    <div class="flex items-center justify-center gap-2">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="text-cyan-400">
        <ellipse cx="12" cy="5" rx="9" ry="3"></ellipse>
        <path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"></path>
        <path d="M21 19c0 1.66-4 3-9 3s-9-1.34-9-3"></path>
      </svg>
      <span class="font-bold tracking-wider text-xs text-center">Enterprise Inputs</span>
    </div>
  </div>

  <div class="flex flex-col gap-3.5 my-auto">
    {inputs.map((item) => (
      <InputCard title={item.title} count={item.count} id={item.id}>
        <slot name="icon" slot="icon">
          {item.icon === 'mail' && (
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
            </svg>
          )}
          {item.icon === 'dollar-sign' && (
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="12" x2="12" y1="2" y2="22"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
            </svg>
          )}
          {item.icon === 'file-text' && (
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"/><path d="M14 2v4a2 2 0 0 0 2 2h4"/><path d="M10 9H8"/><path d="M16 13H8"/><path d="M16 17H8"/>
            </svg>
          )}
          {item.icon === 'globe' && (
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20"/><path d="M2 12h20"/>
            </svg>
          )}
          {item.icon === 'cloud' && (
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M17.5 19H9a7 7 0 1 1 6.71-9h1.79a4.5 4.5 0 1 1 0 9Z"/>
            </svg>
          )}
        </slot>
      </InputCard>
    ))}
  </div>
</div>
